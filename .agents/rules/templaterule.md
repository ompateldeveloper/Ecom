---
trigger: always_on
---

this project is template, dont harcode things, 
make use of libraries like axios, tanstack query ,react hook form.
keep code modular, componetize, use _componets folder if components are not being use out of its own context
